# Legacy Python implementation

This folder contains the previous Python-based Tuya tooling that used to live in the project root.

It is kept for reference only.
It is **not** part of the active GUI or backend anymore.

The supported runtime path for this project is now the Go CLI (`lampctl`).
